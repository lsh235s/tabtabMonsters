using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    //public GameObject objectName; // UI 요소를 포함하는 GameObject에 대한 참조
    //public int baseWidth = 578; // 기본 너비
    //public int baseHeight = 325; // 기본 높이

    //private RectTransform rectTransform; // RectTransform 컴포넌트에 대한 참조

    //void Start()
    //{
    //    // objectName의 RectTransform 컴포넌트에 대한 참조 얻기
    //    rectTransform = objectName.GetComponent<RectTransform>();

    //    // 화면 크기에 따라 UI 요소의 크기와 위치 조정
    //    float rateWidth = (float)Screen.width / baseWidth;
    //    float rateHeight = (float)Screen.height / baseHeight;

    //    float imageWidth = rateWidth * rectTransform.sizeDelta.x;
    //    float imageHeight = rateHeight * rectTransform.sizeDelta.y;

    //    float imageX = rateWidth * rectTransform.anchoredPosition.x;
    //    float imageY = rateHeight * rectTransform.anchoredPosition.y;

    //    rectTransform.sizeDelta = new Vector2(imageWidth, imageHeight);
    //    rectTransform.anchoredPosition = new Vector2(imageX, imageY);

    //    // "DynamicImage" 태그를 가진 모든 GameObject 배열 가져오기
    //    GameObject[] dynamicImages = GameObject.FindGameObjectsWithTag("DynamicImage");

    //    // dynamicImages 배열에 있는 모든 GameObject에 대한 작업 수행
    //    foreach (GameObject dynamicImage in dynamicImages)
    //    {
    //        // 동적 이미지에 대한 추가 작업 수행
    //        // 예: 동적 이미지의 크기 및 위치 조정
    //    }
    //}
}
